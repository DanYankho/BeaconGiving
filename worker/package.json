{
  "name": "church-giving-api",
  "version": "1.0.0",
  "description": "Church Giving Platform — Cloudflare Worker API",
  "main": "src/index.js",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
